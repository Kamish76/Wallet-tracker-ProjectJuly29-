# current bugs that i just saw lol:

current release v0.4.1 -
    during login through google auth, while account is completing its sign in, the no account associated chuchu i frogot, error pop out for a bit then proceeds to open and logs in the app, it looks bad and ew so i should fix that in the following updates

    another bug lols, should double check the whole apps way of calculating total net balance since its not uniform and sometimes does not calculate the right balance since not all transactions are loaded, so you need to scroll down and load all the transactions for it to calculate the actual balance, unlike the widget which does show the real actual balance correctly